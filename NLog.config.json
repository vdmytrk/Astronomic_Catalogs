{
  "NLog": {
    "autoReload": true,
    "throwConfigExceptions": true,
    "targets": {
      "database": {
        "type": "Database",
        "connectionString": "Data Source=DESKTOP-4MV9DF1\\SQLEXPRESS;Initial Catalog=AstroCatalogsDB;Integrated Security=True;Encrypt=False;MultipleActiveResultSets=True;Max Pool Size=200;Password=123vovkUlaka456N2;",
        "commandText": "INSERT INTO NLogs (Timestamp, Level, Logger, Message, Exception) VALUES (@timestamp, @level, @logger, @message, @exception)",
        "parameters": [
          {
            "name": "@timestamp",
            "layout": "${longdate}"
          },
          {
            "name": "@level",
            "layout": "${level}"
          },
          {
            "name": "@logger",
            "layout": "${logger}"
          },
          {
            "name": "@message",
            "layout": "${message}"
          },
          {
            "name": "@exception",
            "layout": "${exception:format=tostring}"
          }
        ]
      }
    },
    "rules": [
      {
        "logger": "*",
        //"minLevel": "Trace",
        "minLevel": "Debug",
        "writeTo": "database"
      }
    ]
  }
}
